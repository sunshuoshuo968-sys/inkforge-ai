# InkForge AI

InkForge AI 是一个面向小说创作的 AI 辅助工作台，提供作品管理、章节编辑、角色与剧情设定、创作记忆以及 AI 生成等功能。

## 本地运行

需要 Node.js，并推荐使用 pnpm：

```bash
pnpm install
pnpm dev
```

生产构建：

```bash
pnpm build
```

AI 服务的 API Key 在应用设置中填写，仅保存在当前浏览器。
